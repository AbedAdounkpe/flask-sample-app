apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: msc-de1-project
  labels:
    app.kubernetes.io/name: flask-app
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  # name only: the selector must keep matching pods after the version label changes
  selector:
    app.kubernetes.io/name: flask-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
